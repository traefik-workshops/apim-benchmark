// Envoy Gateway baseline tests don't use authentication.
// This is a stub so the test runner can load auth.js uniformly for all providers.
const generateKeys = (keyCount) => [];

export { generateKeys };
